<Window x:Class="Snake.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Snake"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight" ContentRendered="Window_ContentRendered" ResizeMode="NoResize"
        KeyUp="Window_KeyUp">

    <Canvas ClipToBounds="True" Width="470" Height="490">
        <StackPanel>
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <Button Width="100" Grid.Column="0" Click="SaveScore_Click">Save score</Button>
            <Button Width="100" Grid.Column="1" Click="LoadScore_Click">Load score</Button>
        </Grid>
        <Border BorderThickness="5" BorderBrush="DarkGreen">
            <Canvas Name="Arena" Width="460" Height="460" ClipToBounds="True">

            </Canvas>
        </Border>
        </StackPanel>
    </Canvas>
</Window>
